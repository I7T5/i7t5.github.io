---
import { getCollection } from "astro:content";
const { tool } = Astro.props;
const toolStr = tool.toLowerCase().split().join("-")

const allProjects = await getCollection("projects")
const tools = [...new Set(allProjects.map((project: any) => project.data.tools).flat())];
let url = (tools.includes(tool)) ? `/tool/${tool}` : "javascript:;"
---

<a class="pr-0.25 font-mono text-0.8rem transition">{tool}</a>

<!-- TODO: implement href and hover:font-bold -->