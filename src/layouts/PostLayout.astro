---
import BaseLayout from "./BaseLayout.astro";
import Tag from "../components/Tag.astro";
import Title from "../components/Title.astro";
const { frontmatter } = Astro.props;
const currYear = new Date().getFullYear()
const postDate = frontmatter.pubDate.toLocaleDateString(undefined, {
    year: (frontmatter.pubDate.getFullYear() == currYear) ? 'numeric' : undefined,
    month: 'short',
    day: 'numeric',
  });
---
<BaseLayout pageTitle={frontmatter.title}>
  <article class="prose transition">
    <h1>{frontmatter.title}</h1>
    <p class="text-secondary">{postDate} · <em>{frontmatter.description}</em></p>
  <!-- <p>Written by {frontmatter.author}</p> -->
  <!-- <img src={frontmatter.image.url} width="300" alt={frontmatter.image.alt} /> -->
    <slot />
  </article>
  <div class="pt-2 mb-4 text-secondary">
    {frontmatter.tags.map((tag: any) => <Tag tag={tag} />)}
  </div>
  <hr>
</BaseLayout>
